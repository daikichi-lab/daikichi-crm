// OpenNext (Cloudflare) 設定。`npm i -D @opennextjs/cloudflare` 後に有効。
// ビルド: npx opennextjs-cloudflare build / デプロイ: npx opennextjs-cloudflare deploy
import { defineCloudflareConfig } from '@opennextjs/cloudflare';
export default defineCloudflareConfig({});
